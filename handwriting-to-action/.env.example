# OpenAI-compatible API key（启用真实手写 OCR 时需要）
OPENAI_API_KEY=

# 可换成兼容网关，例如 Azure / 本地代理
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_VISION_MODEL=gpt-4o
OPENAI_TEXT_MODEL=gpt-4o-mini

# auto | openai | demo
DEFAULT_MODE=auto

HOST=0.0.0.0
PORT=8765
